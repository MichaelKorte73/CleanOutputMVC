{% extends "layout/base.html.twig" %}

{% block content %}
<section class="page-hero page-hero--home">
    <div class="container">

        {% include "elements/2chkbrand.html.twig" %}

        {% include "elements/message.html.twig" %}
        {% include "elements/shorten-form.html.twig" %}

        <p class="hero-hint">Kostenlos. Gönn dir.</p>



        {% if tools is defined %}
        <div class="tool-grid">
            {% for tool in tools %}
                <div class="tool-card">
                    <h3>{{ tool }}</h3>
                </div>
            {% endfor %}
        </div>
        {% endif %}

{{ content|raw }}

<p class="seo-note">
  Kostenloser URL-Shortener zum Kürzen, Prüfen und Teilen von Links.
</p>
    </div>
</section>
{% endblock %}
