{#
    Text Block
    ----------
    Erwartete Variablen:
    - title   : string (optional)
    - content : string (HTML, optional)

    Hinweis:
    - `content` wird bewusst mit `|raw` ausgegeben
      (Controller / BlockRenderer sind für Sicherheit verantwortlich)
#}

<section class="ce ce-text">

    {% if title is defined %}
        <h2>{{ title }}</h2>
    {% endif %}

    {% if content is defined %}
        {{ content|raw }}
    {% endif %}

</section>