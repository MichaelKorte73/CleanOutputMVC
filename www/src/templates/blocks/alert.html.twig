{#
    Alert Block
    -----------
    Erwartete Variablen:
    - level   : string (info | warning | error)
    - content : string (HTML oder Text)
#}

<div
    class="ce ce-alert ce-alert--{{ level|default('info') }}"
    role="alert"
>
    {{ content|raw }}
</div>